# workflow-studio — 2026-03-08


## AI Breakdown: see apps here /home/robos/source/github.com/nddipiazza/

i w — 6:21:14 AM

**Description:** see apps here /home/robos/source/github.com/nddipiazza/

i want to create a new approach.

previously we tried to have a config editor and it was a crappy tree editor with generic like tree like stuff

but the buildbarn-forms-proto is very well defined and we can make real react components for each thing one at a time with a super user friendly interface. 

several oneOf options exist. those should be reallky easy to select UI elements like radio buttons

**Items:** 16

```json
[
  {
    "id": "proto-config-editor-overhaul",
    "type": "epic",
    "title": "Replace Generic Tree Editor with Proto-Driven React Config UI",
    "description": "Retire the crappy generic tree editor and build purpose-built React components for every buildbarn-forms-proto message type. Each proto shape gets its own UX-polished component; oneOf variants are exposed as radio buttons for instant selection.",
    "labels": [
      "type:epic"
    ],
    "steps_to_reproduce": [],
    "children": [
      {
        "id": "spike-proto-schema-audit",
        "type": "spike",
        "title": "Audit buildbarn-forms-proto schema for component inventory",
        "description": "Walk every .proto file in buildbarn-forms-proto and produce a catalogue of all top-level message types, oneOf fields, repeated fields, map fields, and enums. Output a JSON manifest that will drive component generation decisions.",
        "labels": [
          "type:spike"
        ],
        "steps_to_reproduce": [],
        "children": []
      },
      {
        "id": "core-component-library",
        "type": "epic",
        "title": "Build reusable proto-aware base component library",
        "description": "Create the foundation layer: generic, reusable React components that map 1:1 to proto field kinds (scalar, message, repeated, map, oneOf). All higher-level config components are composed from these primitives.",
        "labels": [
          "type:epic"
        ],
        "steps_to_reproduce": [],
        "children": [
          {
            "id": "scalar-field-components",
            "type": "task",
            "title": "Create scalar field components for string, int32/64, bool, bytes, enum",
            "description": "Implement typed input components for each proto scalar kind: text input for string, number spinner for int/float, toggle for bool, hex editor for bytes, and a dropdown for enum. Each validates against proto constraints and shows inline errors.",
            "labels": [
              "type:task"
            ],
            "steps_to_reproduce": [],
            "children": []
          },
          {
            "id": "oneof-radio-selector",
            "type": "task",
            "title": "Create oneOf radio-button selector component",
            "description": "Build a OneOfField component that renders each variant as a labeled radio button. Selecting a variant collapses all other variant forms and expands only the chosen one, with zero-config default values pre-filled.",
            "labels": [
              "type:task"
            ],
            "steps_to_reproduce": [],
            "children": []
          },
          {
            "id": "repeated-field-component",
            "type": "task",
            "title": "Create repeated field list component with add/remove/reorder",
            "description": "Implement a RepeatedField wrapper that renders an ordered list of child field components with '+' add, '×' remove, and drag-to-reorder handles. Suppo
```
