# workflow-studio — 2026-03-09


## AI Breakdown: create the epic breakdown for the attached project's TPS — 12:29:01 AM

**Description:** create the epic breakdown for the attached project's TPS

**Items:** 38

```json
[
  {
    "id": "auth-credential-management",
    "type": "epic",
    "title": "Implement Authentication and Credential Management",
    "description": "Establish secure credential storage and retrieval for all external service integrations, primarily Jira and GitHub, using the `pass` password manager as the canonical secrets backend.",
    "labels": [
      "type:epic"
    ],
    "steps_to_reproduce": [],
    "children": [
      {
        "id": "auth-pass-integration",
        "type": "story",
        "title": "Integrate `pass` password manager for Jira token retrieval",
        "description": "As a developer, I want the agent to retrieve my Jira API token from `pass` so that credentials are never stored in plaintext or environment variables.",
        "labels": [
          "type:story"
        ],
        "steps_to_reproduce": [],
        "children": [
          {
            "id": "auth-pass-retrieval-impl",
            "type": "task",
            "title": "Implement `pass jira/token` retrieval with error handling",
            "description": "Write the credential-fetch utility that calls `pass jira/token`, captures the output, and surfaces actionable error messages when `pass` is not initialized or the key is missing.",
            "labels": [
              "type:task"
            ],
            "steps_to_reproduce": [],
            "children": []
          },
          {
            "id": "auth-pass-validation",
            "type": "task",
            "title": "Add prerequisite validation command for all auth dependencies",
            "description": "Implement a `check prerequisites` command that verifies `pass` is installed, GPG key exists, `jira/token` is stored, and GitHub CLI is authenticated — reporting a clear status for each.",
            "labels": [
              "type:task"
            ],
            "steps_to_reproduce": [],
            "children": []
          }
        ]
      },
      {
        "id": "auth-github-cli",
        "type": "story",
        "title": "Verify GitHub CLI authentication on agent startup",
        "description": "As a developer, I want the agent to confirm `gh auth status` is valid at launch so that PR and issue operations don't fail mid-workflow.",
        "labels": [
          "type:story"
        ],
        "steps_to_reproduce": [],
        "children": [
          {
            "id": "auth-github-startup-check",
            "type": "task",
            "title": "Add GitHub CLI auth check to agent initialization sequence",
            "description": "During `load-agent.sh` startup, run `gh auth status` and halt with an instructive error if the user is not authenticated.",
            "labels": [
              "type:task"
            ],
            "steps_to_reproduce": [],
            "children": []
          }
        ]
      },
      {
        "id": "auth-error-bug",
        "type": "bug",
        "title": "Fix agent crash when `pass` is not installed or GPG key is missing",
        "description": "The
```

## Tasks Created (0) — 12:31:50 AM



## Tasks Created (1) — 1:14:47 AM

- [Fix agent crash when `pass` is not installed or GPG key is missing](https://github.com/nddipiazza/hello-robos/issues/7)

## AI Breakdown: @github.com/Hermetiq/buildbarn-forms can you create a sample — 1:42:56 AM

**Description:** @github.com/Hermetiq/buildbarn-forms can you create a sample issue for this project

**Items:** 6

```json
[
  {
    "id": "knowledge-graph-enhancements",
    "type": "epic",
    "title": "Add Incremental Knowledge Graph Updates",
    "description": "Enable the Apache GenAI agent to update knowledge graphs incrementally rather than rebuilding from scratch on every run. This reduces processing time for large codebases like Apache Tika.",
    "labels": [
      "type:epic"
    ],
    "steps_to_reproduce": [],
    "children": [
      {
        "id": "kg-diff-detection",
        "type": "story",
        "title": "Add Git Diff-Based Change Detection for Knowledge Graph",
        "description": "As a developer, I want the agent to detect which files changed since the last knowledge graph build so that only affected nodes are re-analyzed.",
        "labels": [
          "type:story"
        ],
        "steps_to_reproduce": [],
        "children": [
          {
            "id": "kg-diff-task",
            "type": "task",
            "title": "Implement git diff parser to extract changed file paths",
            "description": "Parse `git diff --name-only <last-kg-commit>..HEAD` output and return a list of affected source files for targeted re-indexing.",
            "labels": [
              "type:task"
            ],
            "steps_to_reproduce": [],
            "children": []
          },
          {
            "id": "kg-cache-task",
            "type": "task",
            "title": "Create knowledge graph node cache with file-hash keys",
            "description": "Store per-file analysis results keyed by SHA-256 hash so unchanged files are skipped during incremental rebuilds.",
            "labels": [
              "type:task"
            ],
            "steps_to_reproduce": [],
            "children": []
          }
        ]
      },
      {
        "id": "kg-stale-node-bug",
        "type": "bug",
        "title": "Fix stale knowledge graph nodes persisting after file deletion",
        "description": "When a source file is deleted from the repository, its corresponding knowledge graph node remains, causing dangling references and incorrect dependency edges.",
        "labels": [
          "type:bug"
        ],
        "steps_to_reproduce": [
          "Clone a sample Apache project (e.g., tika)",
          "Run ./load-agent.sh and execute: build the knowledge graph for /path/to/tika",
          "Delete a source file from the project: rm src/main/java/Foo.java",
          "Re-run the knowledge graph build command",
          "Observe that the deleted file's node still appears in the generated graph output"
        ],
        "children": []
      },
      {
        "id": "kg-perf-spike",
        "type": "spike",
        "title": "Evaluate graph database backends for large-scale Apache codebases",
        "description": "Research whether replacing the current in-memory knowledge graph with a lightweight persistent store (e.g., DuckDB, SQLite-backed graph, or Neo4j) would improve performance and query capability for repositories with 10k+ files.",
   
```

## AI Breakdown: create a single bug task in
 @github.com/Hermetiq/buildbarn- — 2:06:53 AM

**Description:** create a single bug task in
 @github.com/Hermetiq/buildbarn-forms

**Items:** 1

```json
[
  {
    "id": "jsonnet-editor-debounce-unmount-leak",
    "type": "bug",
    "title": "Fix JsonnetEditor debounce timer not cleared on unmount",
    "description": "JsonnetEditor schedules a debounced evaluateJsonnet call on every keystroke but never cancels the timer or guards async state updates when the component unmounts. This causes setState calls on an unmounted component, producing a React warning and a memory leak.",
    "labels": [
      "type:bug"
    ],
    "steps_to_reproduce": [
      "Open src/JsonnetEditor/JsonnetEditor.tsx",
      "Observe that debounceTimer.current is set in scheduleEval but there is no useEffect cleanup calling clearTimeout(debounceTimer.current)",
      "Mount JsonnetEditor with a slow evaluateJsonnet prop (e.g. artificial 500 ms delay)",
      "Type a character to trigger the debounce, then unmount the component within 300 ms (e.g. navigate away in the e2e harness)",
      "Open the browser console and observe: Warning: Can't perform a React state update on an unmounted component"
    ],
    "children": []
  }
]
```

## AI Breakdown: create a single bug in buildbarn-forms as a test — 3:07:51 AM

**Description:** create a single bug in buildbarn-forms as a test

**Items:** 1

```json
[
  {
    "id": "buildbarn-forms-bug-test",
    "type": "bug",
    "title": "Fix test bug in buildbarn-forms",
    "description": "This is a test bug created to verify the issue creation workflow in buildbarn-forms. No actual defect exists; this item is for validation purposes only.",
    "labels": [
      "type:bug"
    ],
    "steps_to_reproduce": [
      "Open the buildbarn-forms repository",
      "Navigate to the main form component",
      "Observe that this is a test bug entry"
    ],
    "children": []
  }
]
```
